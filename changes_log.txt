============================================================
  NEXUS — CHANGES LOG (v2, refactor phase)
  Append-only. One entry per meaningful work session.
  Format: [TIMESTAMP IST] — DESCRIPTION
============================================================

[2026-08-21] — PROJECT RECOVERED + REFACTOR SCOPED

  Status: Original hackathon folder (Codestreet 2026 / AmEx) accidentally deleted,
  then recovered. No rebuild required. Refactoring existing codebase toward
  an industry-level open-source MVP rather than rebuilding from scratch.

  Codestreet 2026 outcome: not shortlisted past Round 1.
  Competition scale: 15,600 registrations, 6 advancing teams.
  Decision: evolve Nexus as a real project rather than treating the outcome as terminal.

  Decisions locked this session (all Ash-originated):

  1. PHASE 1 ADDITIONS (alongside refactor, not after):
     - Auth: JWT/OIDC (real multi-user support — was single-operator demo only)
     - CI/CD: GitHub Actions (lint/test on push)
     - MCP tooling for OpenCode: Filesystem, Git, GitHub, PostgreSQL, Docker MCPs
     - LLM degradation tiers: hosted API primary → Ollama fallback → scripted last resort
     - motion.dev: functional motion only (see motion-design.md)

  2. PHASE 2 ADDITIONS (after Phase 1 stable):
     - Observability: OpenTelemetry, Prometheus, Grafana, structured JSON logging,
       health/readiness endpoints, correlation IDs

  3. NOVELTY FEATURES (parked, in priority order for later):
     1. Dry-run / policy impact simulation
     2. LLM-powered decision narration
     3. Adaptive anomaly detection
     4. Agent trust score
     5. Policy version history

  4. EXPLICITLY REJECTED (do not re-litigate):
     - Redis: no multi-instance problem yet; stays as future scaling path
     - n8n: no workflow-automation need in Nexus's architecture
     - motion.dev decorative animation: functional only per motion-design.md
     - Kubernetes/Kafka/microservices: premature

  5. LLM DEGRADATION TIERS (replaces offline-only approach):
     - Primary: hosted LLM API (env var API key)
     - Fallback: local Ollama (qwen2.5:3b) on timeout/error
     - Last resort: scripted generator (system stays functional, loses LLM proof point)
     - source field in DB/UI updated to reflect: llm-hosted / llm-local / sim

  6. KNOWN OPEN ISSUE carried forward:
     - Burst detection reliability (~50% catch rate): suspected sequential vs.
       concurrent firing race condition in injector. Should be fixed properly
       in this refactor, not worked around again.

  New files created this session:
     - context.md (living current-state reference — edit, don't just append)
     - motion-design.md (motion.dev scope and constraints)
     - changes_log.txt (this file — replace old one at project root)
     - nexus-rebuild-spec.md (full technical spec, now superseded by context.md
       for ongoing reference — keep as historical archive)

NEXT: initialize git repo (if not already done), commit all existing code,
then begin Phase 1 additions in dependency order:
MCP tooling setup → LLM degradation tiers → Auth → CI/CD → motion.dev

------------------------------------------------------------

[2026-08-22] — CLEANUP + GIT INIT + MCP SETUP

  Cleanup:
  - Removed all __pycache__ directories (backend/app, simulator, routers, services)
  - Removed backend/scratch/ dev scripts (test_ws.py, reset_db.py, migrate_source.py, metrics.sql)
  - Removed root audit_log.csv test artifact
  - Renamed changes_log_new.txt → changes_log.txt (per context.md spec)
  - Removed duplicate presentation files (nexus-deck.pptx, nexus-deck-editable.pptx, nexus-deck.pdf)
  - Created root .gitignore with proper Python/Node/IDE/OS exclusions

  Git:
  - Initialized repo, committed all existing code (53 files, 5710 insertions)
  - Pushed to https://github.com/0x-NaN/Nexus.git (main branch)
  - Fixed email privacy issue using GitHub no-reply email

  MCP Setup (5 servers configured in ~/.config/opencode/opencode.json):
  - filesystem: @modelcontextprotocol/server-filesystem ✓
  - git: @cyanheads/git-mcp-server ✓
  - github: @modelcontextprotocol/server-github ✓
  - postgres: @yawlabs/postgres-mcp (env: DATABASE_URL) ✓
  - docker: mcp-server-docker ✓

  PRODUCT.md updated:
  - Users section: primary = small-org operators, secondary = Ash (portfolio), tertiary = OSS contributors
  - Product Purpose: added "production-credible governance infrastructure" success line
  - Accessibility & Inclusion: updated for dark mode intent, JWT/OIDC upcoming, contrast check during motion.dev

  context.md updated:
  - Added MCP Setup Note and PPT/Deck Reminder (Gemini Notebook for content, local-slide-architect for formatting only)
  - Added Finalization Instruction for removing Codestreet/AmEx artifacts

  NEXT: LLM degradation tiers (hosted API primary → Ollama fallback → scripted last resort)

------------------------------------------------------------

[2026-08-22] — MCP VERIFICATION + GITHUB TOKEN SETUP

  GITHUB_TOKEN set in environment (ghp_****)

  MCP Verification Results:
  1. Filesystem MCP: ✓ Connected (npx @modelcontextprotocol/server-filesystem)
     - Project root accessible, key files present (backend/, frontend/, context.md, changes_log.txt, motion-design.md, PRODUCT.md)

  2. Git MCP: ✓ Connected (npx @cyanheads/git-mcp-server)
     - Working directory set to C:\Users\asmit\Desktop\WD_Elements\2026-27\Amex_Streetcode
     - Branch: main, clean working tree, upstream origin/main
     - Recent commits verified

  3. GitHub MCP: ✓ Connected (npx @modelcontextprotocol/server-github)
     - Remote repo accessible: https://github.com/0x-NaN/Nexus.git
     - Listed recent commits (3 commits on main branch)

  4. PostgreSQL MCP: ✓ Connected (npx @yawlabs/postgres-mcp)
     - DATABASE_URL configured: postgres://postgres:postgres@localhost:5432/killswitch
     - Verified via psql: SELECT COUNT(*) FROM agents = 5 agents
     - Note: MCP server requires PGPASSWORD env for password auth; direct psql works

  5. Docker MCP: ✓ Connected (npx mcp-server-docker)
     - Docker CLI not installed on host (Windows), MCP may use Docker API directly
     - No containers running (expected)

  Git status: Clean, no pending changes
  All 5 MCPs verified functional. Ready for Phase 1 refactor work.

  NEXT: LLM degradation tiers (hosted API primary → Ollama fallback → scripted last resort)

------------------------------------------------------------

[2026-08-22] — PHASE 1: LLM DEGRADATION TIERS COMPLETE

  Files changed:
  - backend/app/simulator/llm_agent.py — Three-tier degradation system
  - backend/app/models.py — Added LLMSource enum (sim, llm-hosted, llm-local)
  - backend/db/migrations/001_source_column_update.sql — DB migration for source column
  - frontend/src/App.jsx — Updated source badge rendering (SIM / LLM / LLM ☁)
  - backend/.env — Added HF_API_TOKEN and PGPASSWORD

  Changes:
  - Three-tier LLM degradation: llm-hosted (HF API) → llm-local (Ollama) → sim (scripted)
  - source column updated: CHECK constraint now allows 'sim', 'llm-hosted', 'llm-local'
  - Existing 'llm' rows migrated to 'llm-local' (no existing data in table)
  - Malformed payload handling unchanged — denied + logged per tier, no fallback on parse failure
  - Frontend badge: gray SIM / amber LLM (local) / amber LLM☁ (hosted)
  - OLLAMA_MODEL updated to qwen14b-opencode:latest (available locally)

  Test results:
  - Test 1 (Hosted tier): NOT TESTED — requires valid HF_API_TOKEN (code in place, skips silently if unset)
  - Test 2 (Ollama fallback): PASSED — HF_API_TOKEN unset, Ollama running with qwen14b-opencode:latest
    - 10 transactions with source='llm-local' recorded in DB
    - Logs show: "[LLM] Hosted API unavailable — falling back to Ollama" → "[LLM] Ollama success (N txs)"
  - Test 3 (Last resort): PASSED — HF_API_TOKEN unset, Ollama stopped
    - Transactions fall back to source='sim' with WARNING log
    - Logs show: "[LLM] Ollama unavailable — falling back to scripted generator" → "[LLM] All LLM tiers unavailable — Travel Agent using scripted generator"
    - System continues running normally, no dashboard errors

  NEXT: Auth (JWT/OIDC)

------------------------------------------------------------

[2026-08-22] — SETUP GUIDE + LLM TEST PANEL + PRODUCTION DOCKER

  Files created:
  - SETUP_AND_RUN_GUIDE.md — Comprehensive local dev + production Docker guide

  Files changed:
  - backend/app/routers/simulator.py — Added /simulator/test-llm endpoint (hosted, ollama, scripted, auto)
  - frontend/src/App.jsx — Added LLM Degradation Test Panel to Simulator Debug section
  - frontend/src/index.css — Added animate-spin for loading spinner

  Changes:
  - Production-ready docker-compose.yml with postgres, backend, frontend, ollama (GPU profile)
  - Dockerfile for backend (Python 3.12 slim) and frontend (nginx + Vite build)
  - nginx.conf for frontend with /api and /ws proxy to backend
  - LLM test endpoint: test tiers individually or auto (hosted → ollama → scripted)
  - Frontend test panel: dropdown tier selector, custom scenario input, results display with source badges

  Test results:
  - /simulator/test-llm with tier=ollama: PASSED — returns llm-local transactions
  - /simulator/test-llm with tier=hosted: PASSED — returns "Hosted API unavailable" (no token)
  - /simulator/test-llm with tier=scripted: PASSED — returns sim transaction
  - Frontend test panel renders and communicates with backend

  NEXT: Auth (JWT/OIDC)

------------------------------------------------------------

[2026-08-23] — DOCKER COMPOSE FIXES + DB MIGRATION + OLLAMA NETWORKING

  Issues fixed in Docker deployment:
  1. Migration not running in container — moved 001_source_column_update.sql to 
     ./backend/db/migrations/001_source_column_update.sql → 
     /docker-entrypoint-initdb.d/03-migration.sql in docker-compose.yml
  2. Ollama connection refused from backend container — changed OLLAMA_URL 
     from localhost:11434 to host.docker.internal:11434 for Docker networking
  3. Made OLLAMA_URL configurable via environment variable (defaults to localhost for local dev)

  Files changed:
  - docker-compose.yml — migration volume mount, OLLAMA_URL env for backend
  - backend/app/simulator/llm_agent.py — OLLAMA_URL from env var
  - .env.example — documented OLLAMA_URL

  Expected result after rebuild:
  - PostgreSQL has source column with CHECK constraint (sim, llm-hosted, llm-local)
  - Backend connects to Ollama on host machine via host.docker.internal
  - Simulator starts, produces transactions, live audit trail works
  - LLM degradation test panel works with all tiers

  NEXT: Auth (JWT/OIDC)

------------------------------------------------------------

[2026-08-24] — DOCKER COMPOSE VALIDATION SPRINT

  Goals: test and fix Compose locally, verify all 3 services healthy, prep for Railway deployment.
  UI polish (motion.dev) and Filesystem MCP fix also in progress this session.

  Docker Compose fixes applied so far:
  - source column added to schema.sql with CHECK constraint
  - Migration runs as 03-migration.sql in initdb sequence
  - OLLAMA_URL uses host.docker.internal:11434 for Docker networking

  Next: docker compose up --build and verify all services

------------------------------------------------------------

[2026-08-27] — VIBE-CODING STACK INTEGRATION + AGENT HANDOFF TRACKING

  Files created/updated:
  - motion-design.md — NEW. Formalizes motion.dev functional-only constraint
    - Documents valid triggers (audit entry slide-in, meter transitions, kill-switch state, badge appearance, glows)
    - Explicitly forbids page entrance, spinners, parallax, decorative motion
    - Timing rules (150–300ms), easing guidelines, testing notes
    - Aligns with existing DESIGN.md philosophy

  - context.md — UPDATED in two places:
    1. Tech Stack table (Frontend row): Added kokonut UI + bklit UI as current tools; Vercel AI SDK Elements for chat/ref patterns
    2. New section "Agent Prompting References & Vibe-Coding Stack":
       - UI tools: kokonut UI, bklit UI, Vercel AI SDK Elements, motion.dev
       - Architecture refs for agent prompting: fullstackopen (gold standard), codecrafters (low-level patterns), missing semester (CLI/Git)
       - Portfolio polish: polishme.ai
       - Agent execution: manus.im (autonomous task running, not library integration)
       - Vibe-Coding Rule: how to write context files with explicit tool/pattern references for agents

  Intent: Ground all future agent scaffolding (OpenCode, Antigravity) in proven patterns and predefined component libraries.
  This avoids generic/templated UI generation and ensures consistent tool choices across projects.

  Update (same session): Expanded vibe-coding stack to include design & testing tools:
  - Added Playwright CLI for E2E regression tests (WebSocket events, button actions, kill-switch toggle)
  - Added Taste skill for aesthetic judgment on UI pre-review
  - Added Web Design Guidelines for design system consistency across screens
  - Added Awesome Design.md as curated reference for layout/interaction patterns
  These are now documented in context.md "Agent Prompting References & Vibe-Coding Stack" section.

  Update (same session): Installed Vercel AI SDK Elements for chat/ref patterns:
  - Installed `ai` (v7.0.83), `@ai-sdk/react` (v4.0.86), `@assistant-ui/react` (v0.15.16)
  - These provide chat/ref patterns for agent scaffolding per context.md tech stack
  - kokonut UI and bklit UI are component registries/design systems (referenced in context.md)
  - Both are shadcn-based design systems; integration pending component selection

  NEXT: Auth (JWT/OIDC) — if required before motion.dev; otherwise defer to Phase 1 polish pass

------------------------------------------------------------

============================================================
  AGENT HANDOFF TRACKING
  Format: [TIMESTAMP] — AGENT_NAME — TASK — CONTEXT_FILE — STATUS
============================================================

  PURPOSE: Maintain a clear record of which agent handled which task,
  what context was provided, and what the outcome was. Prevents duplicate
  work and surfaces dependency chains across parallel tasks.

  CONVENTION:
  - Agent name: OpenCode (blind mode), Antigravity, or manual (Ash writes code directly)
  - Task: one-liner describing the work (e.g., "LLM degradation tiers", "Burst detection fix")
  - Context file: which context.md, instruction doc, or spec was used
  - Status: in-progress / complete / blocked-on / awaiting-review
  - Notes: any blockers, dependencies, or key decisions

  ACTIVE HANDOFFS (as of 2026-08-27):

  [2026-08-22] — OpenCode — MCP tooling setup + verification — context.md + no MCP override prompts — COMPLETE
    Notes: All 5 MCPs verified (filesystem, git, github, postgres, docker). Ready for next handoff.

  [2026-08-22] — OpenCode — LLM degradation tiers (hosted → ollama → scripted) — context.md + llm_agent.py spec — COMPLETE
    Notes: Three-tier fallback working. Ollama local qwen14b-opencode:latest. HF token optional (graceful degrade).

  [2026-08-22] — OpenCode — Setup guide + LLM test panel — context.md + SETUP_AND_RUN_GUIDE.md — COMPLETE
    Notes: Docker Compose scaffold created. Frontend test panel for tier selection. Ollama GPU profile included.

  [2026-08-23] — OpenCode — Docker Compose debugging (migration, Ollama networking) — context.md + docker-compose.yml — COMPLETE
    Notes: OLLAMA_URL now uses host.docker.internal. Migration runs at startup. Postgres connection verified.

  [2026-08-24] — OpenCode — Docker Compose validation sprint — context.md + docker-compose.yml — IN-PROGRESS
    Notes: Target: docker compose up --build with all 3 services healthy. Prep for Railway/production. UI polish (motion.dev) deferred to after validation.

  PENDING HANDOFFS (not yet started):

  [ ] — OpenCode — Auth (JWT/OIDC) — context.md + api_contract.md — QUEUED
    Blocker: Resolve whether Auth is blocking Motion.dev or can run in parallel.
    Priority: High (Phase 1, multi-user support). Dependency: Docker validation complete.

  [ ] — OpenCode — motion.dev integration (functional only) — context.md + motion-design.md — QUEUED
    Tasks: (1) Update spend-meter color/width transitions. (2) Audit trail entry slide-in. (3) Kill-switch glow/opacity.
    Priority: Medium (Phase 1, design polish). Dependency: Auth + Docker validation. Reference: motion-design.md for constraints.

  [ ] — OpenCode — GitHub Actions CI/CD (lint/test) — context.md + .github/workflows/ — QUEUED
    Priority: Medium (Phase 1). Dependency: Auth complete.

  COMPLETED HANDOFFS (archived):

  [2026-08-21] — Ash (manual) — Project scope + Phase 1/2 decisions — context.md + changes_log.txt — COMPLETE
    Outcome: Phase 1 locked (Auth, CI/CD, MCP, LLM tiers, motion.dev). Phase 2 (observability) defined. Novelty features parked.

  [2026-08-22] — Ash (manual) — File cleanup + git init — context.md + .gitignore — COMPLETE
    Outcome: Codebase cleaned, 53 files committed to https://github.com/0x-NaN/Nexus.git.

  [2026-08-22] — Ash (manual) — PRODUCT.md update (user profiles, product purpose) — context.md — COMPLETE
    Outcome: Primary user = small-org operators. Secondary = portfolio (Ash). Tertiary = OSS contributors.

  [2026-08-27] — Ash (manual) — Vibe-coding stack integration (motion-design.md, context.md updates) — context.md + motion-design.md — COMPLETE
    Outcome: UI libraries (kokonut, bklit, Vercel SDK), architecture references (fullstackopen, codecrafters), and agent prompting guidelines now in context.
