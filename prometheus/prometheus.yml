global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'nexus-backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['host.docker.internal:8000']
        # If running purely via docker compose, change back to ['backend:8000']
